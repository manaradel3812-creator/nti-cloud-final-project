name: CI Docker + ECR + Helm Deployment

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        required: true
        default: 'nonprod'
        type: choice
        options:
          - nonprod
          - prod

permissions:
  contents: read
  actions: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository مع submodules
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: true

      # 2️⃣ تحميل Terraform outputs
      - name: Download Terraform outputs artifact
        uses: dawidd6/action-download-artifact@v3
        with:
          workflow: platformpipeline.yaml
          name: terraform-outputs
          path: .
          workflow_conclusion: success
          search_artifacts: true
      
      # 3️⃣ Parse Terraform Outputs
      - name: Parse Terraform Outputs
        id: parse_vars
        run: |
          if [ ! -f "tf-outputs.json" ]; then
            echo "❌ tf-outputs.json not found!"
            exit 1
          fi

          # 👇 Kubernetes cluster info
          echo "CLUSTER_NAME=$(jq -r '.eks_cluster_name.value' tf-outputs.json)" >> $GITHUB_ENV
          echo "AWS_REGION=$(jq -r '.aws_region.value // "us-east-1"' tf-outputs.json)" >> $GITHUB_ENV

          # 👇 ECR repository URL dynamic fallback
          ECR_REPO=$(jq -r '
            if has("manar_app_ecr_url") then .manar_app_ecr_url.value
            elif has("ecr_repository_url") then .ecr_repository_url.value
            else empty
            end
          ' tf-outputs.json)

          if [ -z "$ECR_REPO" ]; then
            echo "❌ No ECR repository URL found in Terraform outputs!"
            exit 1
          fi

          echo "ECR_REPOSITORY=$ECR_REPO" >> $GITHUB_ENV
          echo "IMAGE_TAG=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

          # ✅ debug
          echo "ECR_REPOSITORY is set to $ECR_REPO"

      # 4️⃣ Configure AWS Credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 5️⃣ Login to Amazon ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 6️⃣ Optional: Login to Docker Hub (إذا تريد push هناك أيضًا)
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      # 7️⃣ Build, Tag, and Push Docker image to ECR
      - name: Build and Push Image to ECR
        run: |
          # لو الملف باسم Dokerfile غير اسم Dockerfile
          if [ -f "Dokerfile" ]; then mv Dokerfile Dockerfile; fi
          if [ -f "${{ github.workspace }}/appmanar/Dokerfile" ]; then
            mv "${{ github.workspace }}/appmanar/Dokerfile" "${{ github.workspace }}/appmanar/Dockerfile"
          fi
          # Build image
          docker build -t ${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }} "${{ github.workspace }}/appmanar"
          
          # Tag latest
          docker tag ${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }} ${{ env.ECR_REPOSITORY }}:latest

          # Push both tags to ECR
          docker push ${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
          docker push ${{ env.ECR_REPOSITORY }}:latest

      # 8️⃣ Setup Kubernetes Tools
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Setup Helm
        uses: azure/setup-helm@v4

      # 9️⃣ Update kubeconfig
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.CLUSTER_NAME }}

      # 🔟 Helm Upgrade/Install
      - name: Deploy to EKS via Helm
        run: |
          helm upgrade --install manarapp "${{ github.workspace }}/helmchart" \
            --namespace default \
            --create-namespace \
            --set image.repository=${{ env.ECR_REPOSITORY }} \
            --set image.tag=${{ env.IMAGE_TAG }} \
            --set replicaCount=1 \
            --wait --timeout 5m0s
