apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  # 💡 ضفت الـ namespace عشان نضمن إنه ينزل في المكان الصح
  namespace: {{ .Release.Namespace | default "default" }} 
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Release.Name }}
          image: "{{ .Values.image.repository | default (env "ECR_REPOSITORY") }}:{{ .Values.image.tag | default (env "IMAGE_TAG") }}"
          ports:
            - containerPort: 80
          # 👇 الجزء ده "إجباري" للـ Fargate عشان الـ Pod ما يفضلش Pending
          resources:
            requests:
              cpu: "256m"
              memory: "512Mi"
            limits:
              cpu: "256m"
              memory: "512Mi"