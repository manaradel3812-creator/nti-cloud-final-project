replicaCount: 1

image:
  repository: ""
  tag: ""

service:
  type: ClusterIP  # 👈 لازم يكون داخلي عشان الـ NGINX هو اللي يكلمه
  port: 80

ingress:
  enabled: true
  className: nginx  # 👈 لازم يكون nginx عشان البوابة (NGINX) تلقطه
  annotations:
    # مش محتاجين annotations بتاعة AWS هنا لأنها موجودة في الـ Controller نفسه
    kubernetes.io/ingress.class: "nginx"
  hosts:
    - host: ""  # لو معندكيش دومين حقيقي سيبيه فاضي والـ NLB هيشتغل بالـ DNS بتاعه
      paths:
        - path: /
          pathType: Prefix