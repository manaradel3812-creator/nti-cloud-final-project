persistence:
  enabled: true
  storageClass: efs-sc
  size: 20Gi

postgresql:
  persistence:
    enabled: true
    storageClass: efs-sc
    size: 10Gi

resources:
  requests:
    memory: "2Gi"
    cpu: "500m"
  limits:
    memory: "4Gi"
    cpu: "1000m"
