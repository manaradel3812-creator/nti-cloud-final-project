# External Secrets Operator Configuration
installCRDs: true

replicaCount: 1

serviceAccount:
  create: true
  name: external-secrets

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

webhook:
  create: true
